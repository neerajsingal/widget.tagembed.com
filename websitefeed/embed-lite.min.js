var WebsiteFeedApp = WebsiteFeedApp || function (w, u) {
    var AppPath = "https://widget.tagembed.com/websitefeed/embed.json";
    return {
        init: function () {
            fetch(AppPath)
                .then(response => response.json())
                .then(json => {
                    const { css, script } = json;
                    let myCss = document.createElement("link");
                    myCss.setAttribute("type", 'text/css');
                    myCss.setAttribute("rel", 'stylesheet');
                    myCss.setAttribute("href", css);
                    document.head.appendChild(myCss);

                    setTimeout(() => {
                        let myreact = document.createElement("script");
                        myreact.setAttribute("src", script);
                        document.body.appendChild(myreact);
                    }, 1000)

                })
        }
    };
}(window, undefined);
WebsiteFeedApp.init();